fun map(arr: array<T>, mapper: (in: T) => U): array<U> {
  let a: any
  a
}

fun filter(arr: array<T>, predicate: (in: T) => boolean): array<T> {
  let a: any
  a
}

fun reduce(arr: array<T>, reducer: (in: T) => boolean): T {
  let a: any
  a
}
