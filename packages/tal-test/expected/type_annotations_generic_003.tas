fun number_to_string(num: number): string { "str" }

fun map(arr: array<T>, mapper: (in: T) => U): array<U> {
  let a: any
  a
}

let finalArray = map(
  [1, 2, 3]
  in => number_to_string(num: in)
)

finalArray
